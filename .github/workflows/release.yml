name: Release

on:
  release:
    types:
      - published
      - edited
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release-zip:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Prepare release
        run: |
          cd ${{ github.workspace }}/custom_components/wirenboard_discovery
          zip -r ${{ github.workspace }}/wirenboard_discovery.zip ./

      - name: Upload zip
        uses: svenstaro/upload-release-action@v2
        with:
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          file: ${{ github.workspace }}/wirenboard_discovery.zip
          asset_name: wirenboard_discovery.zip
          tag: ${{ github.ref }}
          overwrite: true
